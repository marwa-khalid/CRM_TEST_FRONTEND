// Central route table for the Fleet module. Everything Fleet lives under /fleet/*.
export const FLEET_BASE = "/fleet";

export const FLEET_PATHS = {
  base: FLEET_BASE,
  addHire: FLEET_BASE, // entry screen: Add New Hire (client-side)
};

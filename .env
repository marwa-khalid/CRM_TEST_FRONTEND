# ---------------------------------------------------------------------------
# Shared across ALL environments (loaded in both dev and prod).
# The backend URL is set per-mode and is always "/api" (a same-origin path):
#   .env.development -> `npm run dev`   ; /api is proxied by vite.config.ts
#   .env.production  -> `npm run build` ; /api is proxied by public/_redirects (Netlify)
# Only VITE_-prefixed vars are exposed to the browser.
# ---------------------------------------------------------------------------
VITE_GOOGLE_MAP_KEY=AIzaSyCevjhn71FEm0fsgrmL9x7ZgkHiLa_uzRE
VITE_IDEAL_POSTCODES_KEY=ak_mr3ghag1qHkIUlDfPSO5F6IKO0Q9b
VITE_BACKEND_EMAILAUTH_URL=https://emailbackend-ten.vercel.app
